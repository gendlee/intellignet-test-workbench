<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
    <relativePath/>
  </parent>

  <groupId>com.boc</groupId>
  <artifactId>apitest-backend</artifactId>
  <version>1.0.0</version>
  <name>apitest-backend</name>
  <description>中銀香港智能化 API 測試工作台 — 後端（對齊 docs/API-LIST.md 契約）</description>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- MyBatis-Plus（Spring Boot 3 專用 starter） -->
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
      <version>3.5.7</version>
    </dependency>

    <!-- 默認 profile：H2 記憶體庫（MySQL 兼容模式），零安裝開箱即跑。
         生產 / TDSQL 用 mysql profile（application-mysql.yml，同 schema.sql）。 -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- MySQL 8（TDSQL 可用同一驅動，僅更換連接 URL） -->
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- JDK 23+ 預設停用隱式 annotation processing → 需配合 compiler-plugin <proc>full</proc> -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>1.18.42</version>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- 前端打包進 jar：public/ → BOOT-INF/classes/static/（Spring Boot 自動映射靜態資源），
         shared/ → static/shared/（錄入頁依賴 /shared/diff/xml-parser.js）。
         env.js 排除（部署機器各自放，缺省時前端同源請求即本 jar 的 /api/*）。 -->
    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
      <resource>
        <directory>../public</directory>
        <targetPath>static</targetPath>
        <excludes>
          <exclude>env.js</exclude>
        </excludes>
      </resource>
      <resource>
        <directory>../shared</directory>
        <targetPath>static/shared</targetPath>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <!-- JDK 23+ 預設不啟用 classpath 上的 annotation processors，顯式開啟 -->
          <proc>full</proc>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
