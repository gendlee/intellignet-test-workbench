作为一个顶尖的UI设计+前端工程师, 现在有如下前端设计需求：
1、中银香港智能化API测试工作台
2、支持测试案例的维护：增删改查、人工审核
第一部分（核心）
3、主要的模块：对比银行主机系统API 和 下移到新系统之后的API 输入输出的对比，主机是XML格式报文，新平台是HTTP/Json格式报文；测试案例数据录入有两种方式，一是人工录入（本次使用）、二是自动化测试环境流量接入（预留）。我们以交易码来唯一识别一条测试数据。在录入主机系统的测试案例的时候，可以接入AI自动生成与之对应的HTTP/JSON案例，但预留人工微调的选项，因为迁移到新平台后API会有微调。
4、执行效果，需要比对主机系统和新系统的输出差异，并定位高亮差异字段；需要考虑接口是无状态还是有状态的情况，方便识别差异是否合理。
5、测试案例录入之后是落到数据库，展示的时候能看到最近一次运行结果/时间/执行人等你觉得有必要展示的必要信息；
6、针对已经录入的案例，支持一键批量重跑，实现快速回归测试

第二部分（扩展场景）
7、支持新系统API测试，方式如上
8、支持接口的压力测试设计

第三部分
9、系统是按照应用系统维度的权限开放给客户的（这部分现在默认系统是EBP-CL），如果其他用户则根据用户申请的系统展示对应系统权限，这部分预留展示设计，本次不会做。
10、有入口配置当前系统的URL，URL模式都是固定的，所以支持人性化的填写方式： https://固定.com/固定/差异1/差异2
支持接口请求头的默认配置如API-key等
11、Dashboard展示测试汇总信息
12、支持测试案例导出为Word文档

输出：
HTML页面
API LIST，方便下一步根据API LIST后台实现
前端采用最小依赖原则，不要引入重的框架，最佳选项是 HTML + JSS + CSS 极简框架，与后台的API是HTTP/JSON接口
列出前端需要的技术栈

约束
前端编译环境有nodejs
